export enum FieldTypeEnum {
    Calendar = 'calendar',
    Dropdown = `dropdown`,
    Input = 'input'
}
export enum FieldNameEnum {
    Supplier = `supplier`,
    FtimeOrPtime = `ftimeOrPtime`,
    WorkerType = `workerType`,
    PrbuName = `prbuName`,
    TargetStartDate = `targetStartDate`,
    TargetEndDate = `targetEndDate`
}
